<form action="{{ path('admin_categories_delete', {id: categorie.id}) }}" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet catégorie ?')">
	<input type="hidden" name="token" value="{{ csrf_token('delete' ~ categorie.id) }}">
	<button type="submit" class="button-card-delete">Supprimer</button>
</form>