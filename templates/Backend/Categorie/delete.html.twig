<form action="{{ path('admin_categories_delete', {id: categorie.id}) }}" method="POST" {{stimulus_controller('modal')}}>
	<input type="hidden" name="token" value="{{ csrf_token('delete' ~ categorie.id) }}">
	<button type="submit" class="button button-card-delete">Supprimer</button>
</form>