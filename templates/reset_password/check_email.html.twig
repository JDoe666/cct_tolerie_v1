{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	Mail envoyé
{% endblock %}

{% block body %}
	<section class="container text-center email-sent">
		<div class="email-sent-content">
			<h1>Email envoyé ! <i class="bi bi-check-circle"></i></i></h1>
			<p>
				Si un compte avec cet email existe, alors un email a été envoyé contenant un lien
				que vous pouvez utiliser pour réinitialiser votre mot de passe.
			</p>
			<p>Le lien expirera dans
				{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
			<p>Si vous n'avez pas reçu d'email vérifier vos spams ou renvoyez un mail en cliquant sur le bouton ci-dessous</p>
		</div>
		<div class="text-center">
			<a class="button button-red" href="{{ path('app_forgot_password_request') }}">Essayer a nouveau</a>
		</div>
	</section>

{% endblock %}
