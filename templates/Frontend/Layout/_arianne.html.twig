<div class="ariane">
    <a href="{{path('app_categories')}}">Categories ></a>
    <a href="{{path('app_categories_name', {slug: category.slug})}}">{{ category.name }} </a>
    {% if realisation is defined %}
      <a href="{{path('app_realisations_name', {slugr: realisation.slug, id: realisation.id, slug: category.slug})}}">> {{ realisation.name }}</a>  
    {% endif %}    
</div>